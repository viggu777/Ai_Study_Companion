node_modules
.next
.env
.env.local
.env.*.local
npm-debug.log*
.DS_Store
# Build / eval outputs (generated, not for review)
tsconfig.tsbuildinfo
evaluation-results.json
tests/eval/results.json
tests/eval/history/
.vercel
# Tesseract OCR language data (downloaded at runtime to /tmp on Vercel;
# never commit — local test runs may drop *.traineddata in cwd)
*.traineddata
# Local-only dev docs (design notes, prompt logs, internal reports) —
# kept on disk in extra-docs/ but never pushed to GitHub
/extra-docs/

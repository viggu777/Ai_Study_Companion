# Prompt

No implementation prompt was pre-written for this phase.
`docs/prompting-guide.md` row 17 maps `17-debugging.md` to "(as-needed, not
pre-written)" with done = "N/A".

# Purpose

Reserve the number so the sequence `16 → 17 → 18` reads continuously, and
record where debugging work actually went instead.

# Result

Debugging was done inline per phase (see each prompt's Notes section) plus
the verification pass in `docs/requirements-checklist.md` ("Issues found and
fixed during this verification").

# Changes Made

None (reserved number only).

# Notes

Do not invent a debugging prompt here after the fact — the per-phase Notes
and the checklist are the honest record.

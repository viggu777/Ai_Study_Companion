DATABASE_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# AI providers (split — see lib/ai/AIService.ts header)
# Chat/structured/evaluate via Meta's Llama API (api.llama.com)
META_API_KEY=
META_API_BASE_URL=https://api.llama.com/compat/v1
# Embeddings via Groq (Meta has no embeddings endpoint; verified 404 on /v1/embeddings)
GROQ_API_KEY=
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=
# Admin allow-list (prototype; production would use profiles.is_admin)
# Comma-separated emails and/or user IDs that may access /admin/*
ADMIN_EMAILS=
ADMIN_USER_IDS=